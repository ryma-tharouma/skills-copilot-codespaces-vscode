function skillsMember() {
    var x = calculateNumbers(1, 2);
    var y = calculateNumbers(1, 2, 3);
    var z = calculateNumbers(1, 2, 3, 4); // will throw an error
    console.log(x, y, z);
  }
  

